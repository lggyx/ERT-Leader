<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.9-SNAPSHOT</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>


    <groupId>com.lggyx</groupId>
    <artifactId>ERT-Leader</artifactId>
    <version>0.0.1</version>
    <packaging>pom</packaging>

    <name>ERT-Leader</name>
    <url>https://github.com/lggyx/ERT-Leader</url>
    <description>ERT-Leader</description>

    <!-- 子模块 -->
    <modules>
        <module>ert-leader-common</module>
        <module>ert-leader-pojo</module>
        <module>ert-leader-server</module>
    </modules>

    <!-- 版本 控-->
    <properties>
        <java.version>17</java.version>
        <mybatis.version>3.0.5</mybatis.version>
        <mybatis-plus.version>3.5.14</mybatis-plus.version>
        <knife4j.version>4.5.0</knife4j.version>
        <jwt.version>0.11.5</jwt.version>
        <lombok.version>1.18.42</lombok.version>
        <aop.version>3.1.1</aop.version>
        <ert-leader-common.version>0.0.1</ert-leader-common.version>
        <ert-leader-pojo.version>0.0.1</ert-leader-pojo.version>
        <ert-leader-server.version>0.0.1</ert-leader-server.version>
    </properties>
    <!-- 依赖版本管理 -->
    <dependencyManagement>
        <dependencies>
            <!--mybatis-->
            <dependency>
                <groupId>org.mybatis.spring.boot</groupId>
                <artifactId>mybatis-spring-boot-starter</artifactId>
                <version>${mybatis.version}</version>
            </dependency>
            <dependency>
                <groupId>org.mybatis.spring.boot</groupId>
                <artifactId>mybatis-spring-boot-starter-test</artifactId>
                <version>${mybatis.version}</version>
            </dependency>
            <!-- AOP -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-starter-aop</artifactId>
                <version>${aop.version}</version>
            </dependency>
            <!--knife4j-->
            <dependency>
                <groupId>com.github.xiaoymin</groupId>
                <artifactId>knife4j-openapi3-jakarta-spring-boot-starter</artifactId>
                <version>${knife4j.version}</version>
            </dependency>
            <!--MyBatis-Plus-->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <!-- JWT API -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jwt.version}</version>
            </dependency>
            <!--自定义部分-->
            <dependency>
                <groupId>com.lggyx</groupId>
                <artifactId>ert-leader-common</artifactId>
                <version>${ert-leader-common.version}</version>
            </dependency>
            <dependency>
                <groupId>com.lggyx</groupId>
                <artifactId>ert-leader-pojo</artifactId>
                <version>${ert-leader-pojo.version}</version>
            </dependency>
            <dependency>
                <groupId>com.lggyx</groupId>
                <artifactId>ert-leader-server</artifactId>
                <version>${ert-leader-server.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
    <!--共享依赖-->
    <dependencies>
        <!-- 可选：参数提示 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
        <!-- lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
        </dependency>
    </dependencies>

    <!--资源仓库-->
    <repositories>
        <repository>
            <id>spring-snapshots</id>
            <name>Spring Snapshots</name>
            <url>https://repo.spring.io/snapshot</url>
            <releases>
                <enabled>false</enabled>
            </releases>
        </repository>
    </repositories>

    <!--插件仓库-->
    <pluginRepositories>
        <pluginRepository>
            <id>spring-snapshots</id>
            <name>Spring Snapshots</name>
            <url>https://repo.spring.io/snapshot</url>
            <releases>
                <enabled>false</enabled>
            </releases>
        </pluginRepository>
    </pluginRepositories>

</project>
